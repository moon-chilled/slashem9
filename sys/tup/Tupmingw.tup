CC = x86_64-w64-mingw32-gcc
HOST_CC = cc
SYSSRC = sys/winnt/nttty.c sys/winnt/winnt.c sys/winnt/nh340key.c sys/winnt/ntsound.c
BUILDDIR = build-mingw
EXEEXT = .exe
HOST_EXEEXT =
CFLAGS += -DWIN32CON

include sys/tup/Tupsrc.tup

SLASHEMSRC := $(SRC) $(SYSSRC) $(GENSRC) $(TTY_SRC)

include sys/tup/Tupbase.tup
